<!doctype html>
<html>
<head>
    <title>the next dance</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="profile.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-user"></span>Add Events Page</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6 hide">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>

                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <strong>password</strong>: <%= user.local.password %>
                    </p>

                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>

        </div>
        <div class="col-sm-6">
          <!-- <h3>Events</h3> -->
          <!-- <ul class="messages">
          <% for(var i=0; i<messages.length; i++) {%>
            <li class="message">
              <h5>Name of Event:</h5>
              <span><%= messages[i].msg %></span>
              <h5>Date of event:</h5>
              <span><%= messages[i].date%></span>
              <h5>Details of event:</h5>
              <span><%= messages[i].text%></span> -->
              <!-- <span><%= messages[i].thumbUp %></span> -->
              <!-- <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
              <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span> -->
            <!-- </li>
          <% } %>
          </ul> -->

          <h2>Add An Event</h2>
        <% if (user.local.email) { %>
          <form action="/messages" method="POST">
            <input type="text" placeholder="Event" name="msg">
            <input type="date" value="" name="date">
            <input class="text"type="text" name="text" value="">
            <button type="submit">Submit</button>
          </form>
        </div>
        <% } %>
        <div class="col-sm-6">
          <h3>Choreography</h3>
           <!-- <ul class="messages"> %>
          <% for(var i=0; i<messages.length; i++) {%>
            <video src="videofile.ogg" autoplay poster="posterimage.jpg">

            <li class="message">
              <span><%= messages[i].name %>
              <span><%= messages[i].video %></span>
              <span><%= messages[i].thumbUp %></span>
              <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
              <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
            </video>
          <% } %>
          </ul> -->

          <h2>Upload Your Video</h2>
        <% if (user.local.email) { %>
          <form action="/upload" method="POST" enctype="multipart/form-data">
            <input type="file" id="file" name="video">
            <!-- <button type="submit">Submit</button> -->
          </form>
        </div>
        <% } %>

</div>

<script src="main.js"></script>

</body>
</html>
